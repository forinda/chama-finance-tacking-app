# Postgres connection string for local dev
# Format: postgres://USER:PASSWORD@HOST:PORT/DBNAME
DATABASE_URL=postgres://postgres:postgres@localhost:5432/chama_dev

# 32+ character secret for signing session cookies (generate with: openssl rand -hex 32)
SESSION_SECRET=replace-me-with-a-long-random-string-generated-locally
