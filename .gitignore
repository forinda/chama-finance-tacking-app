.DS_Store
.env
/node_modules/

# React Router
/.react-router/
/build/

# Playwright MCP artifacts (screenshots, console logs, page yaml)
/.playwright-mcp/
*.png
*.jpeg

# One-off local verifier scripts (committed code goes through tests, not these)
*.local.ts
